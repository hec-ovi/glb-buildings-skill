/** The proofs on the document: what rests on what, before any geometry is written. */
export { supports, checkSupport, FLOATING, CANTILEVER, type Support } from './support.ts';
export { area, centroid, inside, overlap, toPoints, type Point } from './polygon.ts';
